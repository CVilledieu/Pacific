#ifndef __UTIL_ERR_H__
#define __UTIL_ERR_H__

// typedef enum {
//     E_NONE = 0,
//     E_FILE_NOT_FOUND,
//     E_OUT_OF_MEMORY,
// } ErrCode;

// typedef enum {
//     ELVL_LOW,
//     ELVL_HIGH,
//     ELVL_CRIT,
// } ErrLevel;

// typedef struct Err_t{
//     char msg[256];
//     char origin[256];
//     ErrLevel severity;
//     ErrCode eType;
// } Err_t;

// typedef union {
//     ErrCode eType;
//     void* data;
// } Error;

// Err_t* newErr(ErrCode type, ErrLevel lvl, const char* msg);

// void printErrlog(void);

#endif